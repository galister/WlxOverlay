namespace X11Overlay.Overlays;

/// <summary>
/// An overlay that supports simple clickable UI elements
/// </summary>
public class UiOverlay : BaseOverlay
{
    public UiOverlay(string key) : base(key)
    {
    }
}